//Import the Scanner class from the java.util package
import java.util.Scanner;

//Define the class name TipCalculator
public class TipCalculator {

    //Define the main method
    public static void main(String[] args) {

        //Create a Scanner to read user input
        Scanner scanner = new Scanner(System.in);

        //Prompt the total bill amount
        System.out.print("Enter the total bill amount: ");
        //Declare a double variable name total bill amount
        double totalBillAmount = scanner.nextDouble();

        //Prompt the percentage of tip
        System.out.print("Enter the percentage of tip: ");
        //Declare a double variable name tip percentage
        double tipPercentage = scanner.nextDouble();

        //Calculate the tip amount
        double tipAmount = totalBillAmount * (tipPercentage/100);
        //Calculate the total amount to pay
        double totalAmount = totalBillAmount + tipAmount;

        //Display the tip amount
        System.out.println("Tip amount: " + tipAmount);
        //Display the total amount to pay
        System.out.println("total amount to pay: " + totalAmount);

        //Close the Scanner object
        scanner.close();

    }
}
