import java.util.Scanner;

public class RestaurantBillCalculator {

    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);

        System.out.print("Enter the total number of people in your table: ");
        int numPeople = scanner.nextInt();

        System.out.print("Enter the total bill amount: ");
        double billAmount = scanner.nextDouble();

        System.out.print("Enter the percentage of tip you want to leave: ");
        double tipPercentage = scanner.nextDouble();

        // Calculate service charge based on number of people
        double serviceCharge;
        if (numPeople <= 4) {
            serviceCharge = billAmount * 0.05;
        } else if (numPeople <= 7) {
            serviceCharge = billAmount * 0.08;
        } else {
            serviceCharge = billAmount * 0.10;
        }

        // Calculate tip amount
        double tipAmount = billAmount * tipPercentage / 100;

        // Calculate total amount to pay
        double totalAmount = billAmount + serviceCharge + tipAmount;

        // Display results
        System.out.println("Service charge amount: $" + serviceCharge);
        System.out.println("Tip amount: $" + tipAmount);
        System.out.println("Total amount to pay: $" + totalAmount);
        
        //Close scanner to prevent leak
         scanner.close();
    }
}
